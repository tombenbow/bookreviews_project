{
  "name": "Backend-project",
  "version": "1.0.0",
  "description": "",
  "main": "listen.js",
  "dependencies": {
    "axios": "^0.19.2",
    "express": "^4.17.1",
    "knex": "^0.21.2",
    "nodemon": "^2.0.4",
    "pg": "^8.3.0",
    "supertest": "^4.0.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.0.1"
  },
  "scripts": {
    "test": "npm run setup-test-db && mocha specs/spec.js",
    "setup-dev-db": "psql -f ./db/dev-setup.sql",
    "setup-test-db": "psql -f ./db/test-setup.sql",
    "seed-dev": "npm run setup-dev-db && knex seed:run",
    "seed-test": "npm run setup-test-db && NODE_ENV=test knex seed:run",
    "start": "npm run seed-dev && nodemon listen.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
